# ✈️ AV2 - Sistema de Gerenciamento GUI

## 📄 Visão Geral do Projeto

Interface gráfica de Usuário (GUI) para gerenciamento de fabricação de aeronaves.


Este projeto é uma **Single Page Application (SPA)** moderna, construída com **React**, focada em fornecer uma interface de usuário limpa e responsiva para o gerenciamento de produção de aeronaves. O sistema visa fornecer uma Interface Gráfica de Usuário, garantindo uma experiência de uso fluida e interativa.

## 💻 Como Executar o Projeto Localmente

Siga os passos abaixo para configurar e executar o projeto em seu ambiente de desenvolvimento.

### 1. Instalação e Execução

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/HerrBriz/AV2.git
    ```

2.  **Acesse o diretório do projeto:**
    ```bash
    cd AV2
    ```

3.  **Instale as dependências:**
    ```bash
    npm install
    ```

4.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```

5.  **Acesse a aplicação:**
    O servidor será iniciado. Abra seu navegador e acesse:
    > **http://localhost:5173**
    
    *(A porta pode variar. Se você usou Create React App, a porta padrão é `3000`. Verifique o output no seu terminal para a URL correta.)*